# ==================== 测试工作流 ====================
# 
# 触发条件：
#   - 推送到所有分支（PR 和 push）
#   - 手动触发
#
# 功能：
#   - 运行类型检查
#   - 运行代码检查（lint、format）
#   - 运行所有测试
#   - 生成测试覆盖率报告
#
# ================================================================

name: Test

on:
  push:
    branches:
      - '**'
    paths:
      - 'packages/**'
      - 'apps/**'
      - 'services/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'turbo.json'
  pull_request:
    branches:
      - '**'
    paths:
      - 'packages/**'
      - 'apps/**'
      - 'services/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'turbo.json'
  # 允许手动触发
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test and Lint
    
    steps:
      # 1. 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 安装 pnpm
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0

      # 3. 设置 Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      # 4. 安装依赖
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 5. 运行类型检查
      - name: Type check
        run: pnpm check:types

      # 6. 运行代码检查（lint 和 format）
      - name: Lint and format check
        run: pnpm check

      # 7. 运行测试
      - name: Run tests
        run: pnpm test

      # 8. 设置 Java（微服务测试）
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      # 9. 启动微服务测试所需基础设施（MySQL、Redis）
      - name: Start infra for microservices tests
        run: docker-compose up -d mysql redis

      # 10. 等待基础设施就绪
      - name: Wait for infra
        run: |
          echo "Waiting for MySQL and Redis..."
          sleep 15
          docker-compose ps mysql redis

      # 11. 运行微服务测试
      - name: Run microservices tests
        run: pnpm services:test

      # 12. 生成测试覆盖率报告
      - name: Generate test coverage
        run: pnpm test:coverage
        continue-on-error: true

      # 13. 上传覆盖率报告到 Artifacts
      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-reports
          path: |
            **/coverage/**
          retention-days: 30

      # 14. 上传覆盖率报告到 Codecov
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: always()
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: |
            **/coverage/lcov.info
            **/coverage/coverage-final.json
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
          verbose: true

      # 15. 测试摘要
      - name: Test Summary
        if: always()
        run: |
          echo "## Test Results ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 测试状态" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 类型检查通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 代码检查通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 前端测试通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 微服务测试通过" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 覆盖率报告" >> $GITHUB_STEP_SUMMARY
          echo "- 📦 覆盖率报告已上传到 [Artifacts](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
          echo "- 📊 覆盖率详情请查看 [Codecov](https://codecov.io/gh/${{ github.repository }})" >> $GITHUB_STEP_SUMMARY
