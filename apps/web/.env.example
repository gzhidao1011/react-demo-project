# API 基础 URL
# 开发环境：如果设置了此变量，将直接使用此 URL（不使用代理）
# 生产环境：必须设置此变量指向生产 API 地址
# 如果不设置，将使用相对路径 /api（通过 Vite 代理转发到 http://localhost:8080）
VITE_API_BASE_URL=http://localhost:8080/api

# Chat 直连模式（可选）
# 设为 true 时，/api/chat 直连 chat-service (localhost:8003)，无需 api-gateway
# 适合快速联调：仅需启动 chat-service 和 user-service（登录用）
# VITE_CHAT_API_DIRECT=true

# 管理员基础路径（可选）
# VITE_ADMIN_BASE_PATH=/admin
