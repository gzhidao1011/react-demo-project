<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.user.mapper.RoleMapper">

    <resultMap id="RoleResultMap" type="com.example.user.entity.RoleEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <sql id="RoleColumns">id, name, code, description, created_at, updated_at, deleted_at</sql>

    <select id="findById" resultMap="RoleResultMap">
        SELECT <include refid="RoleColumns"/> FROM roles WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByIdIncludingDeleted" resultMap="RoleResultMap">
        SELECT <include refid="RoleColumns"/> FROM roles WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="RoleResultMap">
        SELECT <include refid="RoleColumns"/> FROM roles WHERE code = #{code}
        <if test="excludeDeleted">AND deleted_at IS NULL</if>
    </select>

    <select id="findAll" resultMap="RoleResultMap">
        SELECT <include refid="RoleColumns"/> FROM roles
        <if test="!includeDeleted">WHERE deleted_at IS NULL</if>
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM roles
        <if test="!includeDeleted">WHERE deleted_at IS NULL</if>
    </select>

    <select id="existsByCode" resultType="boolean">
        SELECT COUNT(*) > 0 FROM roles WHERE code = #{code} AND deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (name, code, description, created_at, updated_at, deleted_at)
        VALUES (#{name}, #{code}, #{description}, #{createdAt}, #{updatedAt}, #{deletedAt})
    </insert>

    <update id="update">
        UPDATE roles SET name = #{name}, code = #{code}, description = #{description}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="softDelete">
        UPDATE roles SET deleted_at = #{deletedAt}, updated_at = #{deletedAt} WHERE id = #{id}
    </update>

    <update id="restore">
        UPDATE roles SET deleted_at = NULL, updated_at = NOW() WHERE id = #{id}
    </update>
</mapper>
