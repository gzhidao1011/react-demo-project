# ==================== Docker 环境变量配置 ====================
# 复制此文件为 .env 并根据需要修改
# 使用 docker-compose 启动 chat-service 时，需配置 services/.env

# ==================== MySQL 配置 ====================
MYSQL_ROOT_PASSWORD=root123
MYSQL_HOST=mysql
MYSQL_PORT=3306

# ==================== Nacos 配置 ====================
NACOS_SERVER_ADDR=nacos:8848
NACOS_NAMESPACE=public
NACOS_GROUP=DEFAULT_GROUP

# ==================== Sentinel 配置 ====================
SENTINEL_DASHBOARD=sentinel:8858

# ==================== JVM 配置 ====================
JAVA_OPTS=-Xms256m -Xmx512m

# ==================== Redis 配置 ====================
# Redis 密码（留空表示无密码）
REDIS_PASSWORD=

# ==================== LLM 配置（chat-service） ====================
# 未配置时 chat-service 使用 Mock 模式
# LLM_PROVIDER: openai | deepseek（切换大模型）
LLM_PROVIDER=deepseek

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com

# DeepSeek（OpenAI 兼容 API）
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 联网搜索（Function Calling 方式，主流实现）
# 启用后 LLM 在需要实时信息时会调用 web_search 工具
WEB_SEARCH_ENABLED=false
TAVILY_API_KEY=

# ==================== Resend 邮件配置 ====================
# Resend API Key（用于发送验证邮件和密码重置邮件）
# 获取方式：登录 resend.com，在 API Keys 页面创建新的 API Key
RESEND_API_KEY=
# 发件人邮箱配置说明：
# 1. 测试邮箱（onboarding@resend.dev）：只能发送到已验证的邮箱地址
#    限制：只能发送到你的 Resend 账号关联的邮箱地址
# 2. 生产环境：必须在 resend.com/domains 验证域名，然后使用该域名的邮箱地址
#    验证步骤：
#    a) 登录 resend.com/domains
#    b) 添加你的域名（如 example.com）
#    c) 按照提示添加 DNS 记录（SPF、DKIM、DMARC）
#    d) 等待验证通过（通常几分钟到几小时）
#    e) 使用该域名的邮箱地址，如 noreply@example.com
# 3. 临时测试：可以使用已验证的邮箱地址进行测试
RESEND_FROM=onboarding@resend.dev
# 前端应用 URL（用于生成验证链接和密码重置链接）
APP_FRONTEND_URL=http://localhost:5573

# ==================== Admin 账号初始化配置 ====================
# 符合国外主流做法：通过环境变量配置初始 admin 账号
# 仅在首次启动时创建（幂等性），如果 admin 用户已存在则跳过
# 生产环境建议：ADMIN_INIT_ENABLED=false，通过管理界面手动创建 admin 账号
ADMIN_INIT_ENABLED=true
ADMIN_INIT_EMAIL=admin@example.com
ADMIN_INIT_PASSWORD=ChangeMe123!@#
ADMIN_INIT_NAME=Administrator
ADMIN_INIT_PHONE=

# ==================== 日志级别 ====================
LOG_LEVEL=INFO
