<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chat.mapper.ConversationMapper">

    <resultMap id="ConversationResultMap" type="com.example.chat.entity.Conversation">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="ConversationResultMap">
        SELECT id, user_id, title, created_at, updated_at
        FROM conversation
        WHERE id = #{id}
    </select>

    <select id="findByUserIdOrderByUpdatedAtDesc" resultMap="ConversationResultMap">
        SELECT id, user_id, title, created_at, updated_at
        FROM conversation
        WHERE user_id = #{userId}
        ORDER BY updated_at DESC
    </select>

    <insert id="insert">
        INSERT INTO conversation (id, user_id, title, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{title}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE conversation
        SET title = #{title}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM conversation
        WHERE id = #{id}
    </delete>
</mapper>
